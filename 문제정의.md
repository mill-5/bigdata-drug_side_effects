# 프로젝트 문제정의

## 1. 선택한 데이터
- **이름**: 100k Drug Side Effects Dataset
- **출처(URL)**: https://www.kaggle.com/datasets (※ 실제 Kaggle 페이지 URL 재확인 필요)
- **라이선스**: CC0: Public Domain (※ Kaggle 페이지의 라이선스 표기 재대조 필요)
- **크기**: 100,000행 × 16열
- **형식**: CSV

## 2. 풀고 싶은 문제
- **"약물 부작용 100K 데이터로 환자의 인구통계·기저질환·생활습관 정보를 바탕으로 부작용 중증도(Mild / Moderate / Severe)를 예측하고 싶다"**
- **예측/분류 대상(target)**: `severity` (Mild / Moderate / Severe) — 3클래스 분류 문제

## 3. 만들 서비스(완성 모습)
- **입력**: 환자 나이, 성별, 국가, 복용 약물, 복용량, 기저질환 유무, 흡연 여부, 음주 여부
- **출력**: 예상 부작용 중증도(Mild / Moderate / Severe) 확률 + 고위험 경고 메시지
- **ML / 딥러닝 / LLM 중 택1**: **ML** (Random Forest Classifier → Streamlit 대시보드로 시각화)

## 4. 예상 난관 및 해결 전략

### (1) 클래스 불균형
`severity` 컬럼은 Mild가 압도적으로 많고 Severe는 소수이므로 단순 분할만으로는 모델이 Severe를 거의 예측하지 못함.

**해결 전략 (단계적 적용)**
1. **Stratified Shuffle Split** — 3클래스 비율을 유지하는 분할로 검증셋 신뢰도 확보
2. **클래스 가중치 조정 우선 시도** — `RandomForestClassifier(class_weight='balanced')` 또는 `class_weight='balanced_subsample'`
3. **성능 부족 시 SMOTE 추가 비교** — `imblearn.over_sampling.SMOTE(k_neighbors=5)`, 오버샘플링 후 클래스 분포 재확인
   - ※ SMOTE는 원본 분포를 교란할 수 있으므로 가중치 조정과 성능 비교 후 채택

**평가 지표**
- Accuracy 외 **macro F1**, **weighted F1**, **per-class Confusion Matrix** 병행 사용

### (2) 고차원 범주형 변수
국가(`country`)와 약물명(`drug_name`)은 종류가 많아 Label Encoding만으로는 모델이 순서 관계로 오해할 수 있음.

**해결 전략**
- 카디널리티가 낮은 변수 → One-Hot Encoding
- 약물명처럼 종류가 많은 변수 → **Feature Hashing** 또는 약물 카테고리·위험도 그룹으로 압축

### (3) 주요 변수 간 상호작용
복용량(`dosage_mg`)과 기저질환(`chronic_condition`)은 중증도와 상관이 높을 수 있으므로, 두 변수의 교호작용 피처(interaction feature)를 추가하면 모델 성능 개선 가능.

### (4) 데이터셋의 한계 — Synthetic Data
원본 데이터셋은 실제 환자 기록이 아닌 **인공 생성(synthetic) 데이터**로, 각 변수가 독립적·균등하게 랜덤 배정되어 있음.
그 결과 기저질환·약물·성별 등 어떤 기준으로 그룹화해도 흡연율·음주율·중증도 비율이 거의 동일하게 나타나 실질적인 패턴 학습이 어려움.

**해결 전략**
- 의학적 근거 기반으로 변수 간 조건부 확률을 주입해 현실적인 상관관계를 재현한 데이터(`data/drug_side_effects_augmented.csv`)를 생성 (`inject_correlations.py`)
- 원본 데이터는 보존하고 증강 데이터를 모델 학습 및 시각화에 사용
